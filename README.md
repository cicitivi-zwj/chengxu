# chengxu

本仓库由 `git init` 初始化，当前为空项目。

- 默认分支：`master`
- 忽略规则：见 `.gitignore`

## 开始使用

```powershell
git add .
git commit -m "chore: 初始化仓库"
```

## 说明

`.gitignore` 目前是通用模板（覆盖常见操作系统、编辑器、依赖与构建产物）。
确定技术栈后，建议按需精简，只保留与本项目相关的规则。
